# Server
PORT=

# Required in production (Vercel)
JWT_SECRET=
# Turso / libSQL (required on Vercel — local SQLite file won't persist)
# Create a DB at https://turso.tech then paste:
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
# Optional: Vercel Blob for student photos (recommended on Vercel)
# BLOB_READ_WRITE_TOKEN=

# Optional: SMTP to send forgotten passwords by email
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
# SMTP_SECURE=true
